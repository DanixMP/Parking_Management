# راهنمای اپلیکیشن Flutter - سیستم مدیریت پارکینگ

## 📱 نمای کلی

این اپلیکیشن Flutter یک رابط کاربری مدرن و فارسی برای سیستم مدیریت پارکینگ است که با Django API ارتباط برقرار می‌کند.

## 🎨 ویژگی‌های طراحی

### رنگ‌بندی
- **پس‌زمینه اصلی**: `#0F1C2E` (آبی تیره)
- **کارت‌های وضعیت**: 
  - ظرفیت: `#1E3A5F` (آبی)
  - خودروهای داخل: `#2E7D32` (سبز)
  - جای خالی: `#1976D2` (آبی روشن)
  - تعرفه: `#D32F2F` (قرمز)
- **دکمه‌های عملیات**:
  - ثبت ورود: سبز
  - ثبت خروج: قرمز
  - ریست: بنفش

### المان‌های UI
- کارت‌های وضعیت با سایه و آیکون
- دکمه‌های عملیاتی بزرگ و واضح
- جدول فعالیت‌های اخیر
- دیالوگ‌های تنظیمات و ورود/خروج

## 📁 ساختار پروژه

```
lib/
├── main.dart                      # نقطه ورود اپلیکیشن
├── models/                        # مدل‌های داده
│   ├── parking_status.dart
│   ├── entry.dart
│   └── active_car.dart
├── providers/                     # مدیریت state
│   └── parking_provider.dart
├── screens/                       # صفحات اپلیکیشن
│   └── home_screen.dart
├── services/                      # سرویس‌های API
│   └── api_service.dart
└── widgets/                       # کامپوننت‌های قابل استفاده مجدد
    ├── status_card.dart
    ├── action_button.dart
    ├── recent_activity_table.dart
    └── settings_dialog.dart
```

## 🚀 راه‌اندازی

### پیش‌نیازها
1. Flutter SDK نصب شده باشد
2. سرور Django در حال اجرا باشد (`http://localhost:8000`)

### نصب و اجرا

```bash
# رفتن به پوشه پروژه
cd frontend/parking

# دریافت وابستگی‌ها
flutter pub get

# اجرای اپلیکیشن
flutter run
```

### اجرا روی پلتفرم‌های مختلف

```bash
# Windows
flutter run -d windows

# Web
flutter run -d chrome

# Android (با اتصال دستگاه یا emulator)
flutter run -d android

# iOS (فقط روی macOS)
flutter run -d ios
```

## 🔌 اتصال به API

### تنظیم آدرس سرور

فایل: `lib/services/api_service.dart`

```dart
static const String baseUrl = 'http://localhost:8000/api';
```

برای اجرا روی دستگاه واقعی، IP کامپیوتر را جایگزین کنید:
```dart
static const String baseUrl = 'http://192.168.1.100:8000/api';
```

## 📋 قابلیت‌های اپلیکیشن

### 1. نمایش وضعیت پارکینگ
- کل ظرفیت
- تعداد خودروهای داخل
- جای‌های خالی
- تعرفه ساعتی

### 2. ثبت ورود خودرو
- ورود دستی شماره پلاک
- ثبت در دیتابیس
- به‌روزرسانی خودکار وضعیت

### 3. ثبت خروج خودرو
- ورود شماره پلاک
- محاسبه مدت توقف
- محاسبه هزینه
- نمایش رسید

### 4. تنظیمات
- تغییر ظرفیت پارکینگ
- تغییر تعرفه ساعتی

### 5. ریست سیستم
- پاک کردن تمام داده‌ها
- حفظ تنظیمات

### 6. جدول فعالیت‌ها
- نمایش 10 ورود اخیر
- شماره پلاک
- زمان ورود
- آیکون تصویر

## 🔄 مدیریت State

از **Provider** برای مدیریت state استفاده شده است:

```dart
// دسترسی به provider
final provider = Provider.of<ParkingProvider>(context);

// بارگذاری داده‌ها
await provider.loadParkingStatus();
await provider.loadActiveCars();
await provider.loadEntries();

// ثبت ورود
await provider.registerEntry(plate, imagePath);

// ثبت خروج
await provider.registerExit(plate, imagePath);
```

## 🎯 API Endpoints مورد استفاده

| Endpoint | Method | توضیحات |
|----------|--------|---------|
| `/api/status/` | GET | دریافت وضعیت پارکینگ |
| `/api/entries/` | GET | لیست ورودی‌ها |
| `/api/exits/` | GET | لیست خروجی‌ها |
| `/api/active-cars/` | GET | خودروهای داخل پارکینگ |
| `/api/entry/` | POST | ثبت ورود |
| `/api/exit/` | POST | ثبت خروج |
| `/api/settings/` | GET/PUT | دریافت/تغییر تنظیمات |
| `/api/reset/` | POST | ریست دیتابیس |

## 🛠️ توسعه و سفارشی‌سازی

### اضافه کردن صفحه جدید

1. فایل جدید در `lib/screens/` بسازید
2. از `Directionality` برای RTL استفاده کنید
3. به `main.dart` route اضافه کنید

### اضافه کردن ویجت جدید

1. فایل در `lib/widgets/` بسازید
2. از theme colors استفاده کنید
3. در صفحه مورد نظر import کنید

### تغییر رنگ‌ها

فایل: `lib/main.dart`

```dart
theme: ThemeData(
  colorScheme: ColorScheme.fromSeed(
    seedColor: const Color(0xFF1E3A5F), // رنگ اصلی
    brightness: Brightness.dark,
  ),
),
```

## 🐛 عیب‌یابی

### خطای اتصال به سرور

**علت**: سرور Django روشن نیست

**راه‌حل**:
```bash
cd backend
start_django.bat
```

### خطای CORS

**علت**: آدرس frontend در تنظیمات Django نیست

**راه‌حل**: به `backend/parking_api/settings.py` بروید:
```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://localhost:8080",
    # آدرس خود را اضافه کنید
]
```

### داده‌ها نمایش داده نمی‌شوند

**راه‌حل**:
1. دکمه Refresh را بزنید
2. اتصال اینترنت را چک کنید
3. لاگ‌های console را بررسی کنید

## 📱 Build برای Production

### Windows
```bash
flutter build windows --release
```

### Web
```bash
flutter build web --release
```

### Android
```bash
flutter build apk --release
```

## 🔐 نکات امنیتی

1. **آدرس API**: در production از HTTPS استفاده کنید
2. **احراز هویت**: در نسخه‌های بعدی token-based auth اضافه شود
3. **Validation**: همیشه ورودی‌های کاربر را validate کنید

## 📚 وابستگی‌های استفاده شده

```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0              # درخواست‌های HTTP
  provider: ^6.1.1          # مدیریت state
  shamsi_date: ^1.0.1       # تاریخ شمسی
  intl: ^0.19.0             # فرمت اعداد و تاریخ
```

## 🎓 یادگیری بیشتر

- [مستندات Flutter](https://flutter.dev/docs)
- [Provider Package](https://pub.dev/packages/provider)
- [HTTP Package](https://pub.dev/packages/http)

## 📞 پشتیبانی

برای مشکلات و سوالات:
1. لاگ‌های console را بررسی کنید
2. مستندات API را مطالعه کنید
3. کد نمونه‌ها را بررسی کنید

---

**وضعیت**: ✅ آماده برای استفاده
**آخرین به‌روزرسانی**: 30 نوامبر 2025
