# 🚗 راهنمای کامل سیستم مدیریت پارکینگ

## 📋 فهرست مطالب
1. [نمای کلی سیستم](#نمای-کلی-سیستم)
2. [معماری](#معماری)
3. [راه‌اندازی سریع](#راه‌اندازی-سریع)
4. [مستندات کامل](#مستندات-کامل)
5. [عیب‌یابی](#عیب‌یابی)

---

## نمای کلی سیستم

سیستم مدیریت پارکینگ شامل سه بخش اصلی است:

### 1. Backend (Python/Django)
- **مسیر**: `backend/`
- **تکنولوژی**: Django 5.2.8 + Django REST Framework
- **دیتابیس**: SQLite (`src/parking.db`)
- **پورت**: 8000

### 2. Frontend (Flutter)
- **مسیر**: `frontend/parking/`
- **تکنولوژی**: Flutter 3.10+
- **زبان**: Dart
- **پشتیبانی**: Windows, Web, Android, iOS

### 3. Legacy System (Python/PyQt)
- **مسیر**: `backend/src/`
- **تکنولوژی**: PyQt5 + YOLO
- **قابلیت**: تشخیص پلاک با دوربین

---

## معماری

```
┌─────────────────────────────────────────────────────────┐
│                    Flutter Frontend                      │
│              (Persian UI - Material Design)              │
│                  Port: Dynamic/Web                       │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP/REST API
                     ↓
┌─────────────────────────────────────────────────────────┐
│                   Django REST API                        │
│              (API Endpoints + CORS)                      │
│                    Port: 8000                            │
└────────────────────┬────────────────────────────────────┘
                     │ ORM/Direct SQL
                     ↓
┌─────────────────────────────────────────────────────────┐
│                  SQLite Database                         │
│                  (parking.db)                            │
│  Tables: entries, exits, active_cars, settings          │
└─────────────────────────────────────────────────────────┘
                     ↑
                     │ Direct Access
┌─────────────────────────────────────────────────────────┐
│              Legacy Python System                        │
│         (YOLO Detection + PyQt GUI)                      │
└─────────────────────────────────────────────────────────┘
```

---

## راه‌اندازی سریع

### گام 1: راه‌اندازی Backend

```bash
# ترمینال 1
cd backend
DjangoEnv\Scripts\activate
python manage.py runserver 8000
```

✅ سرور باید روی `http://localhost:8000` اجرا شود

### گام 2: راه‌اندازی Frontend

```bash
# ترمینال 2
cd frontend/parking
flutter pub get
flutter run
```

✅ اپلیکیشن Flutter باز می‌شود و به API متصل می‌شود

### گام 3: تست سیستم

1. در Flutter اپ، دکمه Refresh را بزنید
2. کارت‌های وضعیت باید داده نمایش دهند
3. یک ورود تستی ثبت کنید
4. یک خروج تستی ثبت کنید

---

## ساختار پروژه

```
Parking/
├── backend/                      # Backend Django
│   ├── DjangoEnv/               # Virtual environment
│   ├── parking_api/             # Django project
│   │   ├── settings.py          # تنظیمات (CORS, DB, etc.)
│   │   └── urls.py              # URL routing
│   ├── api/                     # API application
│   │   ├── models.py            # Database models
│   │   ├── serializers.py       # JSON serializers
│   │   ├── views.py             # API endpoints
│   │   └── urls.py              # API routes
│   ├── src/                     # Legacy system
│   │   ├── database.py          # Database functions
│   │   ├── parking.db           # SQLite database
│   │   ├── detect_entry.py      # Entry detection
│   │   ├── detect_exit.py       # Exit detection
│   │   └── *.pt                 # YOLO models
│   ├── manage.py                # Django management
│   └── start_django.bat         # Quick start script
│
├── frontend/                    # Frontend Flutter
│   └── parking/
│       ├── lib/
│       │   ├── main.dart        # Entry point
│       │   ├── models/          # Data models
│       │   ├── providers/       # State management
│       │   ├── screens/         # UI screens
│       │   ├── services/        # API service
│       │   └── widgets/         # Reusable widgets
│       └── pubspec.yaml         # Dependencies
│
└── Documents/                   # مستندات
    ├── API_DOCUMENTATION.md
    ├── DJANGO_README.md
    ├── FLUTTER_APP_GUIDE.md
    ├── INTEGRATION_GUIDE.md
    └── COMPLETE_SYSTEM_GUIDE.md (این فایل)
```

---

## API Endpoints

### وضعیت و اطلاعات
| Endpoint | Method | توضیحات |
|----------|--------|---------|
| `/api/status/` | GET | وضعیت فعلی پارکینگ |
| `/api/settings/` | GET | دریافت تنظیمات |
| `/api/settings/` | PUT | تغییر تنظیمات |

### مدیریت خودروها
| Endpoint | Method | توضیحات |
|----------|--------|---------|
| `/api/entry/` | POST | ثبت ورود |
| `/api/exit/` | POST | ثبت خروج |
| `/api/active-cars/` | GET | خودروهای داخل |

### سوابق
| Endpoint | Method | توضیحات |
|----------|--------|---------|
| `/api/entries/` | GET | تمام ورودی‌ها |
| `/api/exits/` | GET | تمام خروجی‌ها |

### مدیریت
| Endpoint | Method | توضیحات |
|----------|--------|---------|
| `/api/reset/` | POST | ریست دیتابیس |

---

## قابلیت‌های سیستم

### ✅ Backend (Django API)
- [x] REST API با Django REST Framework
- [x] CORS برای اتصال Frontend
- [x] مدل‌های دیتابیس
- [x] Serialization به JSON
- [x] استفاده از database.py موجود
- [x] مدیریت تنظیمات
- [x] ریست دیتابیس

### ✅ Frontend (Flutter)
- [x] UI فارسی (RTL)
- [x] طراحی مدرن و تیره
- [x] کارت‌های وضعیت
- [x] دکمه‌های عملیاتی
- [x] جدول فعالیت‌ها
- [x] دیالوگ تنظیمات
- [x] مدیریت State با Provider
- [x] خطایابی و Retry
- [x] Refresh دستی و خودکار

### ✅ Legacy System
- [x] تشخیص پلاک با YOLO
- [x] GUI با PyQt5
- [x] مدیریت دیتابیس
- [x] گزارش‌گیری

---

## مستندات کامل

### برای Backend
📄 **API_DOCUMENTATION.md** - مستندات کامل API
- تمام endpoint‌ها
- نمونه request/response
- کدهای خطا
- نمونه‌های curl و Python

📄 **DJANGO_README.md** - راهنمای Django
- نصب و راه‌اندازی
- ساختار پروژه
- تنظیمات CORS
- توسعه و Debug

📄 **DJANGO_SETUP_COMPLETE.md** - خلاصه نصب
- چک‌لیست نصب
- محیط توسعه
- گام‌های بعدی

### برای Frontend
📄 **FLUTTER_APP_GUIDE.md** - راهنمای Flutter
- ساختار پروژه
- راه‌اندازی
- توسعه
- Build برای Production

### برای یکپارچه‌سازی
📄 **INTEGRATION_GUIDE.md** - اتصال Frontend + Backend
- تنظیمات شبکه
- تست اتصال
- سناریوهای استفاده
- عیب‌یابی

---

## عیب‌یابی

### مشکل: Flutter به API متصل نمی‌شود

**علائم**:
- خطای "Connection refused"
- صفحه خطا در Flutter

**راه‌حل**:
1. بررسی کنید سرور Django روشن است:
   ```bash
   curl http://localhost:8000/api/status/
   ```
2. اگر خطا داد، سرور را روشن کنید:
   ```bash
   cd backend
   start_django.bat
   ```

### مشکل: خطای CORS

**علائم**:
- خطای CORS در console مرورگر
- درخواست‌ها blocked می‌شوند

**راه‌حل**:
در `backend/parking_api/settings.py`:
```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://localhost:8080",
    # آدرس Flutter خود را اضافه کنید
]
```

### مشکل: دیتابیس خالی است

**علائم**:
- کارت‌ها 0 نمایش می‌دهند
- جدول خالی است

**راه‌حل**:
```bash
cd backend/src
python init_database.py
```

### مشکل: Flutter dependencies نصب نمی‌شوند

**راه‌حل**:
```bash
cd frontend/parking
flutter clean
flutter pub get
```

---

## دستورات مفید

### Backend
```bash
# شروع سرور
python manage.py runserver 8000

# ایجاد superuser
python manage.py createsuperuser

# بررسی مشکلات
python manage.py check

# تست API
python test_api.py
```

### Frontend
```bash
# نصب dependencies
flutter pub get

# اجرا
flutter run

# اجرا روی Windows
flutter run -d windows

# اجرا روی Web
flutter run -d chrome

# Build
flutter build windows --release
flutter build web --release
```

### Database
```bash
# مشاهده دیتابیس
sqlite3 backend/src/parking.db
.tables
SELECT * FROM entries;
.quit
```

---

## نکات مهم

### امنیت
⚠️ این تنظیمات برای Development است
- در Production از HTTPS استفاده کنید
- SECRET_KEY را تغییر دهید
- DEBUG را False کنید
- از PostgreSQL استفاده کنید

### Performance
- برای داده‌های زیاد از Pagination استفاده کنید
- Cache را فعال کنید
- Static files را optimize کنید

### Backup
- دیتابیس را به صورت دوره‌ای backup بگیرید
- تصاویر را در مکان امن ذخیره کنید

---

## گام‌های بعدی

### پیشنهادات توسعه

1. **احراز هویت**
   - اضافه کردن login/logout
   - مدیریت کاربران
   - سطوح دسترسی

2. **گزارش‌گیری**
   - گزارش روزانه/ماهانه
   - نمودارها و آمار
   - Export به Excel/PDF

3. **اتصال به YOLO**
   - یکپارچه‌سازی با detect_entry.py
   - نمایش تصاویر در Flutter
   - تشخیص خودکار

4. **اعلان‌ها**
   - اعلان وقتی پارکینگ پر شد
   - اعلان برای خودروهای طولانی‌مدت
   - Push notifications

5. **پرداخت آنلاین**
   - درگاه پرداخت
   - صدور فاکتور
   - تاریخچه پرداخت‌ها

---

## پشتیبانی

### منابع یادگیری
- [Django Documentation](https://docs.djangoproject.com/)
- [Django REST Framework](https://www.django-rest-framework.org/)
- [Flutter Documentation](https://flutter.dev/docs)
- [Provider Package](https://pub.dev/packages/provider)

### فایل‌های مهم
- `backend/parking_api/settings.py` - تنظیمات Django
- `backend/api/views.py` - منطق API
- `frontend/parking/lib/services/api_service.dart` - سرویس API
- `frontend/parking/lib/screens/home_screen.dart` - صفحه اصلی

---

## وضعیت پروژه

✅ **Backend**: آماده و کامل
✅ **Frontend**: آماده و کامل  
✅ **Integration**: تست شده و کار می‌کند
✅ **Documentation**: کامل و جامع

**آخرین به‌روزرسانی**: 30 نوامبر 2025

---

## تماس و پشتیبانی

برای سوالات و مشکلات:
1. مستندات را مطالعه کنید
2. بخش عیب‌یابی را بررسی کنید
3. لاگ‌های خطا را چک کنید
4. کد نمونه‌ها را بررسی کنید

**موفق باشید! 🚀**
