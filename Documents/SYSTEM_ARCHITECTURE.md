# 🏗️ معماری سیستم - System Architecture

## نمای کلی سیستم / System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Flutter Desktop Application                  │  │
│  │                                                           │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │  │
│  │  │  Status  │  │  Entry   │  │   Exit   │  │Settings │ │  │
│  │  │  Cards   │  │  Dialog  │  │  Dialog  │  │ Dialog  │ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │         Recent Activity Table                      │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP/REST API
                              │ JSON
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        BACKEND LAYER                            │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  Django REST API                          │  │
│  │                                                           │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │  │
│  │  │  Status  │  │  Entry   │  │   Exit   │  │Settings │ │  │
│  │  │Endpoint  │  │Endpoint  │  │ Endpoint │ │Endpoint │ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │         CORS Middleware                            │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ SQL Queries
                              │ ORM
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                               │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  SQLite Database                          │  │
│  │                   (parking.db)                            │  │
│  │                                                           │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │  │
│  │  │ entries  │  │  exits   │  │  active  │  │settings │ │  │
│  │  │  table   │  │  table   │  │   cars   │  │  table  │ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↑                                  │
│                              │ Direct Access                    │
│                              │                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Legacy Python System                         │  │
│  │                                                           │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │  │
│  │  │  YOLO    │  │  PyQt5   │  │ Database │  │ Reports │ │  │
│  │  │Detection │  │   GUI    │  │ Functions│  │ Module  │ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## جریان داده / Data Flow

### 1. ثبت ورود خودرو / Vehicle Entry Registration

```
┌──────────┐
│  User    │
│  Input   │
└────┬─────┘
     │ 1. Enter plate number
     ↓
┌──────────────┐
│   Flutter    │
│   Dialog     │
└────┬─────────┘
     │ 2. POST /api/entry/
     │    {plate: "12ب345-67"}
     ↓
┌──────────────┐
│   Django     │
│   API View   │
└────┬─────────┘
     │ 3. Call database.register_entry()
     ↓
┌──────────────┐
│  Database    │
│  Function    │
└────┬─────────┘
     │ 4. INSERT INTO entries
     │ 5. INSERT INTO active_cars
     ↓
┌──────────────┐
│   SQLite     │
│   Database   │
└────┬─────────┘
     │ 6. Return entry_id
     ↓
┌──────────────┐
│   Django     │
│   Response   │
└────┬─────────┘
     │ 7. JSON: {success: true, entry_id: 1}
     ↓
┌──────────────┐
│   Flutter    │
│   Update UI  │
└──────────────┘
```

### 2. ثبت خروج خودرو / Vehicle Exit Registration

```
┌──────────┐
│  User    │
│  Input   │
└────┬─────┘
     │ 1. Enter plate number
     ↓
┌──────────────┐
│   Flutter    │
│   Dialog     │
└────┬─────────┘
     │ 2. POST /api/exit/
     │    {plate: "12ب345-67"}
     ↓
┌──────────────┐
│   Django     │
│   API View   │
└────┬─────────┘
     │ 3. Call database.register_exit()
     ↓
┌──────────────┐
│  Database    │
│  Function    │
└────┬─────────┘
     │ 4. SELECT from active_cars
     │ 5. Calculate duration & cost
     │ 6. INSERT INTO exits
     │ 7. DELETE FROM active_cars
     ↓
┌──────────────┐
│   SQLite     │
│   Database   │
└────┬─────────┘
     │ 8. Return result
     ↓
┌──────────────┐
│   Django     │
│   Response   │
└────┬─────────┘
     │ 9. JSON: {duration: 120, cost: 40000}
     ↓
┌──────────────┐
│   Flutter    │
│  Show Result │
└──────────────┘
```

---

## ساختار کامپوننت‌ها / Component Structure

### Flutter Frontend

```
lib/
├── main.dart                    # Entry Point
│   └── MyApp
│       └── MaterialApp
│           └── ChangeNotifierProvider
│               └── HomeScreen
│
├── screens/
│   └── home_screen.dart
│       ├── StatusCards (4x)
│       ├── ActionButtons (3x)
│       └── RecentActivityTable
│
├── widgets/
│   ├── status_card.dart         # Reusable status card
│   ├── action_button.dart       # Reusable action button
│   ├── recent_activity_table.dart
│   └── settings_dialog.dart
│
├── providers/
│   └── parking_provider.dart    # State Management
│       ├── loadParkingStatus()
│       ├── registerEntry()
│       ├── registerExit()
│       └── updateSettings()
│
├── services/
│   └── api_service.dart         # HTTP Client
│       ├── getParkingStatus()
│       ├── registerEntry()
│       ├── registerExit()
│       └── updateSettings()
│
└── models/
    ├── parking_status.dart
    ├── entry.dart
    └── active_car.dart
```

### Django Backend

```
backend/
├── parking_api/
│   ├── settings.py              # Configuration
│   │   ├── INSTALLED_APPS
│   │   ├── MIDDLEWARE (CORS)
│   │   ├── DATABASES
│   │   └── CORS_ALLOWED_ORIGINS
│   │
│   └── urls.py                  # Main routing
│       └── include('api.urls')
│
├── api/
│   ├── models.py                # Database Models
│   │   ├── Entry
│   │   ├── Exit
│   │   ├── ActiveCar
│   │   └── Setting
│   │
│   ├── serializers.py           # JSON Serializers
│   │   ├── EntrySerializer
│   │   ├── ExitSerializer
│   │   ├── ActiveCarSerializer
│   │   └── ParkingStatusSerializer
│   │
│   ├── views.py                 # API Logic
│   │   ├── parking_status()
│   │   ├── register_entry_api()
│   │   ├── register_exit_api()
│   │   └── settings_view()
│   │
│   └── urls.py                  # API routing
│       ├── /status/
│       ├── /entry/
│       ├── /exit/
│       └── /settings/
│
└── src/
    └── database.py              # Database Functions
        ├── register_entry()
        ├── register_exit()
        ├── get_capacity()
        └── count_active_cars()
```

---

## مدل داده / Data Model

```
┌─────────────────────────────────────────────────────────────┐
│                         entries                             │
├─────────────────────────────────────────────────────────────┤
│ id (PK)          │ INTEGER │ Auto increment                 │
│ plate            │ TEXT    │ License plate number           │
│ image_in         │ TEXT    │ Entry image path               │
│ timestamp_in     │ TEXT    │ Entry timestamp                │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 1:1
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                       active_cars                           │
├─────────────────────────────────────────────────────────────┤
│ entry_id (PK,FK) │ INTEGER │ Reference to entries.id        │
│ plate            │ TEXT    │ License plate number           │
│ timestamp_in     │ TEXT    │ Entry timestamp                │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 1:N
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                          exits                              │
├─────────────────────────────────────────────────────────────┤
│ id (PK)          │ INTEGER │ Auto increment                 │
│ entry_id (FK)    │ INTEGER │ Reference to entries.id        │
│ plate            │ TEXT    │ License plate number           │
│ image_out        │ TEXT    │ Exit image path                │
│ timestamp_out    │ TEXT    │ Exit timestamp                 │
│ duration_minutes │ INTEGER │ Parking duration               │
│ cost             │ INTEGER │ Calculated cost                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        settings                             │
├─────────────────────────────────────────────────────────────┤
│ key (PK)         │ TEXT    │ Setting name                   │
│ value            │ TEXT    │ Setting value                  │
├─────────────────────────────────────────────────────────────┤
│ capacity         │ "200"   │ Total parking capacity         │
│ price_per_hour   │ "20000" │ Hourly rate in Tomans         │
│ last_reset       │ "..."   │ Last reset timestamp           │
└─────────────────────────────────────────────────────────────┘
```

---

## State Management / مدیریت State

```
┌──────────────────────────────────────────────────────────┐
│                   ParkingProvider                        │
│                  (ChangeNotifier)                        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  State Variables:                                        │
│  ├── ParkingStatus? _status                             │
│  ├── List<Entry> _entries                               │
│  ├── List<ActiveCar> _activeCars                        │
│  ├── bool _isLoading                                     │
│  └── String? _error                                      │
│                                                          │
│  Methods:                                                │
│  ├── loadParkingStatus()                                │
│  ├── loadEntries()                                       │
│  ├── loadActiveCars()                                    │
│  ├── registerEntry(plate, imagePath)                    │
│  ├── registerExit(plate, imagePath)                     │
│  ├── updateSettings(capacity, price)                    │
│  └── resetDatabase()                                     │
│                                                          │
│  Notifications:                                          │
│  └── notifyListeners() → Updates all widgets            │
└──────────────────────────────────────────────────────────┘
                         │
                         │ Provides state to
                         ↓
┌──────────────────────────────────────────────────────────┐
│                    Consumer Widgets                      │
├──────────────────────────────────────────────────────────┤
│  ├── HomeScreen                                          │
│  ├── StatusCards                                         │
│  ├── ActionButtons                                       │
│  └── RecentActivityTable                                 │
└──────────────────────────────────────────────────────────┘
```

---

## Network Communication / ارتباط شبکه

```
Flutter App (Client)                Django API (Server)
─────────────────────               ───────────────────

┌─────────────────┐                 ┌─────────────────┐
│  HTTP Request   │                 │  URL Router     │
│                 │                 │                 │
│  GET /status/   │────────────────>│  urls.py        │
│                 │                 │                 │
│  Headers:       │                 │  Route to view  │
│  Content-Type:  │                 │                 │
│  application/   │                 ↓                 │
│  json           │                 ┌─────────────────┐
└─────────────────┘                 │  View Function  │
                                    │                 │
                                    │  parking_status │
                                    │                 │
                                    │  1. Query DB    │
                                    │  2. Serialize   │
                                    │  3. Return JSON │
                                    └─────────────────┘
                                            │
┌─────────────────┐                         │
│  HTTP Response  │<────────────────────────┘
│                 │
│  Status: 200    │
│  Content-Type:  │
│  application/   │
│  json           │
│                 │
│  Body:          │
│  {              │
│    capacity: 200│
│    active: 45   │
│    free: 155    │
│    price: 20000 │
│  }              │
└─────────────────┘
```

---

## Security & CORS / امنیت و CORS

```
┌──────────────────────────────────────────────────────────┐
│                    CORS Flow                             │
└──────────────────────────────────────────────────────────┘

Flutter App                         Django Server
───────────                         ─────────────

1. Preflight Request (OPTIONS)
   ┌─────────────────┐
   │ OPTIONS /api/   │──────────────>┌─────────────────┐
   │ Origin:         │               │ CORS Middleware │
   │ localhost:3000  │               │                 │
   └─────────────────┘               │ Check origin    │
                                     │ in allowed list │
                                     └─────────────────┘
                                             │
   ┌─────────────────┐                       │
   │ 200 OK          │<──────────────────────┘
   │ Access-Control- │
   │ Allow-Origin:   │
   │ localhost:3000  │
   └─────────────────┘

2. Actual Request
   ┌─────────────────┐
   │ GET /api/status/│──────────────>┌─────────────────┐
   │ Origin:         │               │ Process Request │
   │ localhost:3000  │               │                 │
   └─────────────────┘               └─────────────────┘
                                             │
   ┌─────────────────┐                       │
   │ 200 OK + Data   │<──────────────────────┘
   │ Access-Control- │
   │ Allow-Origin:   │
   │ localhost:3000  │
   └─────────────────┘
```

---

## Deployment Architecture / معماری استقرار

### Development
```
┌────────────────────────────────────────────────────────┐
│                  Developer Machine                     │
│                                                        │
│  ┌──────────────┐         ┌──────────────┐           │
│  │   Flutter    │         │   Django     │           │
│  │   Desktop    │◄───────►│   Dev Server │           │
│  │   App        │  HTTP   │   Port 8000  │           │
│  └──────────────┘         └──────────────┘           │
│                                   │                   │
│                                   ↓                   │
│                           ┌──────────────┐           │
│                           │   SQLite     │           │
│                           │   parking.db │           │
│                           └──────────────┘           │
└────────────────────────────────────────────────────────┘
```

### Production (Suggested)
```
┌────────────────────────────────────────────────────────┐
│                    Client Devices                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │
│  │ Windows  │  │   Web    │  │  Mobile  │            │
│  │ Desktop  │  │ Browser  │  │   App    │            │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘            │
└───────┼─────────────┼─────────────┼───────────────────┘
        │             │             │
        └─────────────┴─────────────┘
                      │ HTTPS
                      ↓
┌────────────────────────────────────────────────────────┐
│                   Web Server (Nginx)                   │
│                   SSL/TLS Termination                  │
└────────────────────────────────────────────────────────┘
                      │
                      ↓
┌────────────────────────────────────────────────────────┐
│              Application Server (Gunicorn)             │
│                    Django + DRF                        │
└────────────────────────────────────────────────────────┘
                      │
                      ↓
┌────────────────────────────────────────────────────────┐
│              Database Server (PostgreSQL)              │
│                  or SQLite for small scale             │
└────────────────────────────────────────────────────────┘
```

---

**نکته**: این معماری برای محیط توسعه طراحی شده است. برای محیط تولید، تنظیمات امنیتی و مقیاس‌پذیری بیشتری نیاز است.
